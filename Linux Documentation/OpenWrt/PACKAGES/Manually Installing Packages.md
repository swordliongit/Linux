1. Extract the ipk file with
```sh
tar zxpvf package_name.ipk
```

2. Put the data.tar.gz into the /tmp directory and run the command in the root directory. This will install the package:
```sh
cd /
tar zxpvf tmp/data.tar.gz
```